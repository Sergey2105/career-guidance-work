import React from "react";
import Center from "@/assets/shared/components/layout/Center";
import NotFoundError from "@/assets/shared/components/NotFoundError";

const NotFound = () => {
    return (
        <Center title="NotFound">
            <NotFoundError />
        </Center>
    );
};
export default NotFound;
